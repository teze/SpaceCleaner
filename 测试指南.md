# CleanMyMac 测试指南

## 快速测试流程

### 1. 构建和启动
```bash
# 构建应用
./build-unified.sh

# 启动应用
./run.sh
```

### 2. 测试大文件扫描器（已修复）

**测试目的**：验证大文件扫描器可以找到 > 100MB 的文件

**步骤**：
1. 启动应用
2. 在左侧边栏点击"大文件"
3. 点击"开始扫描"按钮
4. 等待扫描完成（可能需要几分钟，取决于文件数量）
5. 查看扫描结果

**预期结果**：
- ✅ 应该能找到用户目录下 > 100MB 的文件
- ✅ 表格显示文件名称、大小、路径
- ✅ 状态栏显示"找到 X 个项目，共 XXX"
- ✅ "清理"按钮变为可用状态

**常见大文件位置**：
- `~/Downloads/` - 下载的大文件
- `~/Movies/` - 视频文件
- `~/Library/Application Support/` - 应用数据
- `~/Documents/` - 文档和项目文件

### 3. 测试旧文件扫描器（已修复）

**测试目的**：验证旧文件扫描器可以找到 > 30 天未使用的文件

**步骤**：
1. 在左侧边栏点击"旧文件"
2. 点击"开始扫描"按钮
3. 等待扫描完成
4. 查看扫描结果

**预期结果**：
- ✅ 应该能找到 > 30 天未修改且 > 1MB 的文件
- ✅ 表格显示文件名称、大小、路径
- ✅ 状态栏显示"找到 X 个项目，共 XXX"
- ✅ "清理"按钮变为可用状态

### 4. 测试清理功能（新功能）

**⚠️ 警告**：清理功能会将文件移到废纸篓。建议先在测试目录测试！

**准备测试环境**：
```bash
# 创建测试目录
mkdir -p ~/Desktop/CleanMyMac测试

# 创建一些测试文件
dd if=/dev/zero of=~/Desktop/CleanMyMac测试/大文件1.bin bs=1m count=150
dd if=/dev/zero of=~/Desktop/CleanMyMac测试/大文件2.bin bs=1m count=200

# 创建旧文件（修改时间为 60 天前）
touch -t 202603010000 ~/Desktop/CleanMyMac测试/旧文件1.txt
dd if=/dev/zero of=~/Desktop/CleanMyMac测试/旧文件1.txt bs=1m count=2
```

**测试步骤**：
1. 在"大文件"模块扫描 `~/Desktop/CleanMyMac测试`
   - 注意：当前扫描整个用户目录，无法指定目录
   - 可以在扫描结果中找到测试文件
2. 点击"清理"按钮
3. 查看确认对话框：
   - ✅ 显示项目数量
   - ✅ 显示可释放空间
   - ✅ 提示文件会被移到废纸篓
4. 点击"清理"确认
5. 等待清理完成
6. 查看清理结果对话框：
   - ✅ 显示成功清理的数量
   - ✅ 如果有失败，显示失败原因
7. 验证文件已移到废纸篓：
   ```bash
   ls -la ~/.Trash/
   ```

**预期结果**：
- ✅ 清理过程中显示进度："清理中... (X/Y)"
- ✅ 清理完成后显示结果对话框
- ✅ 文件已移到废纸篓（可以在 Finder 的废纸篓中看到）
- ✅ 扫描列表已清空或更新
- ✅ 可以从废纸篓恢复文件

### 5. 测试系统垃圾清理

**步骤**：
1. 在左侧边栏点击"系统垃圾"
2. 点击"开始扫描"按钮
3. 等待扫描完成
4. 查看扫描结果（系统缓存、日志等）
5. 点击"清理"按钮
6. 确认清理
7. 查看清理结果

**预期结果**：
- ✅ 能找到系统缓存文件
- ✅ 清理功能正常工作
- ✅ 文件移到废纸篓

### 6. 测试智能扫描

**步骤**：
1. 在左侧边栏点击"智能扫描"
2. 点击"开始扫描"按钮
3. 等待扫描完成（会扫描多个类别）
4. 查看扫描结果

**预期结果**：
- ✅ 显示找到的项目总数
- ✅ 显示可释放的空间总量
- ✅ 扫描过程显示"正在扫描..."
- ✅ 扫描完成显示"扫描完成！"

**注意**：智能扫描目前只显示统计，不支持直接清理。需要到各个模块单独清理。

### 7. 测试 Space Lens

**步骤**：
1. 在左侧边栏点击"空间透镜"
2. 点击"选择文件夹"按钮
3. 选择一个目录（建议选择 `~/Documents` 或 `~/Downloads`）
4. 等待扫描完成
5. 查看树状图可视化
6. 点击矩形区域深入探索
7. 点击"返回上级"按钮

**预期结果**：
- ✅ 树状图正确显示目录结构
- ✅ 矩形大小与文件/目录大小成正比
- ✅ 颜色按深度区分
- ✅ 鼠标悬停时高亮显示
- ✅ 点击可以进入子目录
- ✅ 面包屑导航显示当前路径
- ✅ 返回按钮正常工作

## 性能测试

### 扫描性能
- **小目录**（< 1000 文件）：应该在 1-2 秒内完成
- **中等目录**（1000-10000 文件）：应该在 5-10 秒内完成
- **大目录**（> 10000 文件）：可能需要 30 秒到几分钟

### 内存使用
```bash
# 查看应用内存使用
ps aux | grep CleanMyMac
```

**预期**：内存使用应该 < 200 MB

### 应用大小
```bash
# 查看应用大小
du -sh build/CleanMyMac.app
```

**预期**：应用大小约 300 KB

## 错误处理测试

### 1. 权限错误
**测试**：尝试清理系统保护的文件
**预期**：显示错误消息，不会崩溃

### 2. 文件不存在
**测试**：扫描后删除文件，然后尝试清理
**预期**：显示错误消息，继续清理其他文件

### 3. 磁盘空间不足
**测试**：在磁盘空间不足时尝试清理
**预期**：显示错误消息

## 已知限制

1. **扫描范围**：
   - 大文件和旧文件扫描整个用户目录
   - 无法指定自定义扫描路径
   - 扫描深度限制为 5 层

2. **清理功能**：
   - 当前清理所有扫描到的项目，不支持选择性清理
   - 未来版本将支持勾选单个项目

3. **权限**：
   - 某些系统目录需要管理员权限
   - 应用不会自动请求管理员权限

4. **性能**：
   - 大目录扫描可能较慢
   - 未来版本将优化多线程扫描

## 清理测试文件

测试完成后，清理测试文件：
```bash
# 从废纸篓恢复文件（如果需要）
# 在 Finder 中打开废纸篓，右键点击文件，选择"放回原处"

# 或者清空废纸篓
rm -rf ~/.Trash/*

# 删除测试目录
rm -rf ~/Desktop/CleanMyMac测试
```

## 报告问题

如果发现问题，请记录：
1. 操作步骤
2. 预期结果
3. 实际结果
4. 错误消息（如果有）
5. 系统版本和应用版本

---

**祝测试顺利！** 🎉
