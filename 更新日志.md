# CleanMyMac 更新日志

## 2026-05-30 - v1.1

### ✅ 已修复
1. **大文件扫描器修复**
   - 问题：大文件模块点击"开始扫描"后找不到文件
   - 原因：使用了错误的扫描方法 `scan(category:)` 而不是 `scanLargeFiles()`
   - 解决：在 `ViewControllers.swift` 中修改 `startScan()` 方法，根据类别调用正确的扫描方法
   - 现在大文件扫描器可以正确找到 > 100MB 的文件

2. **旧文件扫描器修复**
   - 同样的问题，现在使用 `scanOldFiles()` 方法
   - 可以正确找到 > 30 天未使用且 > 1MB 的文件

### 🎉 新功能
1. **真正的清理功能**
   - 实现了 `SystemScanner.cleanup()` 方法
   - 文件会被移到废纸篓（可恢复），而不是永久删除
   - 清理进度实时显示
   - 清理完成后显示成功/失败统计
   - 如果有失败项，会显示错误原因
   - 清理成功后自动刷新列表

2. **改进的清理确认对话框**
   - 显示将要清理的项目数量
   - 显示可释放的空间大小
   - 明确提示文件会被移到废纸篓

3. **清理结果反馈**
   - 全部成功：显示成功消息，清空列表
   - 部分失败：显示成功/失败数量和错误原因，自动重新扫描

### 📊 当前功能状态

#### ✅ 完全可用
- **智能扫描**：一键扫描所有类别，显示可释放空间
- **Space Lens**：树状图可视化磁盘空间，交互式导航
- **系统垃圾**：扫描 + 清理系统缓存、日志、临时文件
- **大文件**：扫描 + 清理 > 100MB 的文件
- **旧文件**：扫描 + 清理 > 30 天未使用的文件

#### ⏳ 占位符（即将推出）
- **应用卸载**：完全卸载应用及其相关文件
- **隐私清理**：清理浏览器历史、Cookie、下载记录

### 🔧 技术改进
1. **错误处理**：清理失败时显示详细错误信息
2. **用户体验**：清理过程中显示进度，禁用按钮防止重复操作
3. **安全性**：使用 `FileManager.trashItem()` 而不是直接删除

### 📝 代码变更
- `CleanMyMacUnified/Scanner.swift`：添加 `cleanup()` 方法
- `CleanMyMacUnified/ViewControllers.swift`：
  - 修复 `startScan()` 方法，根据类别调用正确的扫描器
  - 完善 `startClean()` 方法，实现真正的清理功能

### 🎯 下一步计划
1. **应用卸载器**
   - 扫描 /Applications 和 ~/Applications
   - 查找应用相关文件（Preferences, Application Support, Caches 等）
   - 实现完全卸载功能

2. **隐私清理**
   - Safari 历史和 Cookie
   - Chrome 历史和 Cookie
   - Firefox 历史和 Cookie
   - 下载历史
   - 最近使用的文件

3. **重复文件检测**
   - 基于 MD5/SHA256 的内容检测
   - 智能分组和预览
   - 批量删除重复文件

4. **性能优化**
   - 多线程扫描优化
   - 大目录扫描性能改进
   - 内存使用优化

### 📦 构建信息
- **版本**：1.1
- **构建日期**：2026-05-30
- **应用大小**：约 300 KB
- **最低系统**：macOS 13.0+
- **架构**：ARM64 (Apple Silicon)

### 🚀 使用方法
```bash
# 构建
./build-unified.sh

# 运行
./run.sh

# 或直接打开
open build/CleanMyMac.app
```

### ⚠️ 注意事项
1. 首次运行可能需要授予文件访问权限
2. 某些系统目录可能需要管理员权限
3. 清理前请确认要删除的文件
4. 文件会被移到废纸篓，可以恢复

---

## 历史版本

### 2026-05-30 - v1.0
- 初始版本
- 实现基础框架和 UI
- 实现智能扫描、Space Lens、系统垃圾、大文件、旧文件模块
- 仅支持扫描，不支持清理
